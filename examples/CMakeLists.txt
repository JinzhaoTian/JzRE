# JzRE Examples
# This directory contains example applications demonstrating JzRE usage
# Note: EditorExample is maintained as a standalone CMake project and is not
# added to the root build graph.

add_subdirectory(RuntimeExample)
